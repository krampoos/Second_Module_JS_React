const initialState = {
    email: '',
    pass: '',
    fileUrl: null,
    file: null,
    user: null,
    progress: ''
};

const newsReducer = (state = initialState, action) => {
    return state
};

export default newsReducer;